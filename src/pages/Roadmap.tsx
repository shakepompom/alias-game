import React from 'react';
import { Roadmap } from '@features';
import { AppWrapper } from '@components';

const RoadmapPage = (): JSX.Element => {
  return (
    <AppWrapper>
      <Roadmap />
    </AppWrapper>
  );
};

export default RoadmapPage;
